<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.gnastnosaj.boilerplate.push">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <service
            android:name=".service.PushService"
            android:process=":push" />

        <receiver android:name=".receiver.TickAlarmReceiver" />

        <receiver android:name=".receiver.BootCompletedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.ConnectivityChangeReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.PushBroadcastReceiver">
            <intent-filter>
                <action android:name="com.github.gnastnosaj.boilerplate.push" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.Json2NotificationPushBroadcastReceiver">
            <intent-filter>
                <action android:name="com.github.gnastnosaj.boilerplate.push.json2notification" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
